PERSIST STATE ON STATE ENTER, NOT STATE COMPLETE!!!!!!!!!!!!!!!!!!!!

MUST FIX
1) make outbox polling not apart of state machine
2) make listening for messages not apart of the state machine, and it has to see orderId and invoke the right step
3) make services that do the database persistence and stuff, state machine shouldnt do it directly. Like OrdersService and maybe InventoryService that sends the rabbitmq message

